import { MentornamePipe } from './mentorname.pipe';

describe('MentornamePipe', () => {
  it('create an instance', () => {
    const pipe = new MentornamePipe();
    expect(pipe).toBeTruthy();
  });
});
